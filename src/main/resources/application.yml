spring:
  application:
    name: locadora
  datasource:
    url: jdbc:postgresql://localhost:5434/locadoradb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      show-sql: true
#      ddl-auto: update
      ddl-auto: none

    database-platform: org.hibernate.dialect.PostgreSQLDialect